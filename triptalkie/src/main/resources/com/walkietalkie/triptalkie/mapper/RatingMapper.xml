<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.walkietalkie.triptalkie.mapper.RatingMapper">
	<select id="findMateList"
		resultType="com.walkietalkie.triptalkie.domain.Rating">
		SELECT mk.title,
		ml.member_id AS memberId,
		mk.enddate
		FROM makemate mk
		JOIN memberlist ml ON mk.idx = ml.makemate_idx
		WHERE
		mk.member_id = #{hostId};
	</select>

	<insert id="insertRating"
		parameterType="com.walkietalkie.triptalkie.domain.Star">
		INSERT INTO star (
		rater_id, rated_id, rating, makemate_idx, israted
		) VALUES (
		#{raterId}, #{ratedId}, #{rating}, #{makemateIdx}, #{israted}
		)
	</insert>

</mapper>