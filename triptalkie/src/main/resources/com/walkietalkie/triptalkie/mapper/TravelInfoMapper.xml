<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.walkietalkie.triptalkie.mapper.TravelInfoMapper">
	<insert id="registerTravelInfo" parameterType="com.walkietalkie.triptalkie.domain.TravelInfo">
		INSERT INTO travelinfo (title, infotype, infodate, city_id, content, member_id) VALUES (#{title}, #{infotype}, #{infodate}, #{cityId}, #{content}, #{memberId})
	</insert>
	<select id="findTravelInfoAllList" resultType="com.walkietalkie.triptalkie.domain.TravelInfo">
		select idx, title, view, created_at, updated_at, infotype, infodate, city_id, content, member_id from travelinfo
	</select>
	<select id="findTravelInfoIdx" resultType="com.walkietalkie.triptalkie.domain.TravelInfo">
		select idx, title, view, created_at, updated_at, infotype, infodate, city_id, content, member_id from travelinfo where idx=#{idx}
	</select>
	<update id="updateTravelInfoByIdxAndMemberId" parameterType="com.walkietalkie.triptalkie.domain.TravelInfo">
		update travelinfo set title=#{title}, infotype=#{infotype}, infodate=#{infodate}, city_id=#{cityId}, content=#{content}, updated_at = NOW()  where idx=#{idx} and member_id=#{memberId}
	</update>
	<delete id="deleteTravelInfoByIdx">
		delete from travelinfo where idx = #{idx} and member_id=#{memberId}
	</delete>
	<select id="findTravelInfoTop3">
		SELECT idx, title, member_id FROM travelinfo ORDER BY view DESC LIMIT 3
	</select>
	
	<select id="findTravelInfoByMemberId">
		SELECT idx, title, member_id, created_at FROM travelinfo WHERE member_id=#{memberId} ORDER BY created_at DESC
	</select>
	
</mapper>